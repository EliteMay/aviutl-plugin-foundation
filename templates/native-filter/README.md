# Native Filter Starter

Target artifact: `.auf2`.

This directory is a starter contract placeholder. The SDK-backed source template will be added after the Hub can detect the compiler, Windows SDK, CMake and AviUtl2 SDK version reliably.
